Edit ,|sort|uniq
Edit X/sharot/D
New draft
du -n | grep -v '^\.git'
du -n | grep Aide
du | sed 's,^.* ,,' | grep -v '^\.git'
git
git add Aide
git add .
git commit -a -m 'Limit point increases/decreases on a share or side.  Should be part of stars?'
git diff
git fetch
git log --decorate --graph --oneline --all | sed 10q
git log --decorate --oneline | sed 5q
git push origin master
git rebase origin/master
log
log git
ls -F | mc
