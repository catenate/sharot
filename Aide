Edit ,|sort|uniq
du -n | grep -v '^\.git'
du -n | grep Aide
du | sed 's,^.* ,,' | grep -v '^\.git'
git
git add .
git commit -a -m 'Spaced out card back.'
git diff
git fetch
git log --decorate --graph --oneline --all | sed 10q
git log --decorate --oneline | sed 10q
git push origin master
git rebase origin/master
log
log git
ls -F | mc
